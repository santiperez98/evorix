import { chatbotEngine } from './ChatbotEngine';

// Simulación de uso:
const userInput = 'Hola, quiero saber sobre desarrollo web';
const reply = chatbotEngine(userInput);
console.log('Bot:', reply);
